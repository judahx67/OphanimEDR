# Ophanim-EDR Causality Engine — Python dependencies
# Install: pip install -r requirements.txt

# Core ML
torch>=2.0.0          # Neural network framework (CPU or CUDA)
numpy>=1.24.0         # Numerical computation

# Graph neural networks (optional — fallbacks provided)
# torch-geometric must be installed with --no-deps on Windows
# torch-sparse and torch-scatter fail to build; pure-PyTorch fallbacks are used
# torch-geometric>=2.4.0

# Visualization (optional)
matplotlib>=3.7.0     # Score distribution plots

# Notebook
jupyter>=1.0.0        # Running .ipynb files
